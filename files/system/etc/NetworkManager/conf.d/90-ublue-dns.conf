# ublue-hyprland: keep DNS pinned to the local filtering resolver.
#
# NetworkManager must not rewrite /etc/resolv.conf with DHCP-provided servers
# (that would bypass the blocker). dns=none = NM leaves resolv.conf alone.
[main]
dns=none
